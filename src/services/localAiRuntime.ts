import { PoseLandmark } from '../types';
export interface LocalPersonAnalysis {detected:boolean;confidence:number;latency_ms:number;landmarks:PoseLandmark[];faces:Array<{x:number;y:number;width:number;height:number;confidence:number}>;face_mesh?:Array<Array<{index:number;x:number;y:number;z:number}>>;segmentation?:{foreground_ratio:number;mean_confidence:number}|null;body?:Record<string,number|null>;capabilities:Record<string,boolean>;notes:string[];}
const BASE=import.meta.env.VITE_FITAI_API_URL||'http://127.0.0.1:8000';
async function checked(r:Response){if(!r.ok)throw new Error(`${r.status} ${r.statusText}: ${await r.text()}`);return r;}
export async function analyzeImage(file:Blob):Promise<LocalPersonAnalysis>{const body=new FormData();body.append('file',file,'frame.jpg');const r=await checked(await fetch(`${BASE}/api/v1/person/analyze`,{method:'POST',body}));return r.json();}
export async function analyzeVideoFrame(video:HTMLVideoElement):Promise<LocalPersonAnalysis>{if(video.readyState<2||!video.videoWidth||!video.videoHeight)throw new Error(`Video not ready: readyState=${video.readyState}, size=${video.videoWidth}x${video.videoHeight}`);const maxW=640;const scale=Math.min(1,maxW/video.videoWidth);const c=document.createElement('canvas');c.width=Math.round(video.videoWidth*scale);c.height=Math.round(video.videoHeight*scale);const ctx=c.getContext('2d');if(!ctx)throw new Error('Canvas unavailable');ctx.drawImage(video,0,0,c.width,c.height);const blob=await new Promise<Blob|null>(resolve=>c.toBlob(resolve,'image/jpeg',0.78));if(!blob)throw new Error('Frame encoding failed');return analyzeImage(blob);}
export async function runtimeHealth(){return (await checked(await fetch(`${BASE}/api/v1/health`,{cache:'no-store'}))).json();}
